<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Password Reset - Macautoindia</title>
    <style>
      /* Basic resets */
      body,
      table,
      td,
      p,
      a {
        -webkit-text-size-adjust: 100%;
        -ms-text-size-adjust: 100%;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
          Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji";
      }
      img {
        -ms-interpolation-mode: bicubic;
        border: 0;
        height: auto;
        outline: none;
        text-decoration: none;
      }
      a {
        text-decoration: none;
      }

      /* Main container */
      .email-container {
        max-width: 600px;
        margin: 20px auto;
        border: 1px solid #e0e0e0;
        border-radius: 8px;
        overflow: hidden; /* Ensures border-radius is respected */
      }

      /* Header */
      .header {
        background-color: #4062ae; /* NEW: Primary Blue */
        padding: 30px 20px;
        text-align: center;
      }
      .logo {
        color: #ffffff;
        font-size: 28px;
        font-weight: 700; /* Bolded for emphasis */
        margin: 0;
        text-decoration: none;
      }

      /* Content Area */
      .content {
        background-color: #ffffff;
        padding: 40px 30px;
      }
      .greeting {
        color: #1a202c; /* Darker text */
        font-size: 22px;
        font-weight: 600;
        margin: 0 0 20px 0;
      }
      .intro-text {
        color: #333333; /* Standard dark gray */
        font-size: 16px;
        line-height: 1.6;
        margin: 0 0 25px 0;
      }

      /* Reset Button (CTA) */
      .reset-button {
        display: inline-block;
        background-color: #86c867; /* NEW: Action Green */
        color: #ffffff !important; /* Important for email client compatibility */
        text-decoration: none;
        padding: 14px 28px;
        border-radius: 6px;
        font-size: 16px;
        font-weight: 600;
        border: none; /* Removed box-shadow for a cleaner look */
        margin: 15px 0;
        transition: background-color 0.2s ease;
      }
      .reset-button:hover {
        background-color: #76b85b; /* NEW: Darker Green for hover */
      }

      /* Support & Footer */
      .support-text {
        color: #4a5568;
        font-size: 15px;
        margin-top: 30px;
        line-height: 1.6;
      }
      .support-email {
        color: #4062ae; /* NEW: Primary Blue for links */
        text-decoration: none;
        font-weight: 600;
      }
      .footer {
        background-color: #4062ae; /* NEW: Primary Blue */
        padding: 30px 20px;
        text-align: center;
      }
      .footer-text {
        color: #e2e8f0; /* Lighter text for contrast on blue */
        font-size: 14px;
        margin: 5px 0 0 0;
      }
      .footer-link {
        color: #86c867; /* NEW: Action Green accent */
        text-decoration: none;
        font-weight: 600;
      }
    </style>
  </head>
  <body style="margin: 0; padding: 0; background-color: #f7fafc">
    <div class="email-container">
      <div class="header">
        <h1 class="logo">Macautoindia.com</h1>
        </div>

      <div class="content">
        <p class="greeting">Hello, </p>
        <p class="intro-text">
          We received a request to reset your password. If you made this request,
          click the button below to set a new password:
        </p>

        <p style="text-align: center">
          <a href="<%= resetLink %>" class="reset-button">🔒 Reset Password</a>
        </p>

        <p class="intro-text" style="margin-top: 25px">
          This password reset link will expire in 60 minutes. If you didn’t
          request a password reset, you can safely ignore this email.
        </p>

        <p class="support-text">
          Need help? Contact our support team at
          <a href="mailto:contact@Macautoindia.com" class="support-email"
            >contact@Macautoindia.com</a
          >.
        </p>
      </div>

      <div class="footer">
        <p class="footer-text">Stay secure,</p>
        <p class="footer-text" style="font-weight: 600; color: #ffffff">
          Team Macautoindia
        </p>
        <p class="footer-text" style="margin-top: 15px">
          <a href="https://mack-ev.com" class="footer-link">www.mack-ev.com</a>
        </p>
      </div>
    </div>
  </body>
</html>